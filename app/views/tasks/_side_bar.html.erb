

<div class="lists" >
<% current_user.lists.each do |list| %>
  <div class="list" data-id= <%=list.id%> id="list-name"> 
    <div class="list-name" >
      <%= link_to list.name, list_tasks_path(list), id:"list_path"  %>
    </div>
    <div class= delete-btn>
      <%= link_to image_tag("img_x2.png"), list_path(list), method: :delete%>
    </div>
  </div>
<% end%>
  

  <div class="create-list">
   <ul class="create-list-btn">
   <li><%= link_to "リスト作成", new_list_path, method: :get, class: "create-list"%></li>
   </ul>
   </div>
</div>